# BodyRecomp CHANGELOG
## v6.8.3 (2026-07-16)

### 日期切页流畅度修复
- 日期内容切换完全复用底部主 Tab 的动画参数：300ms、轻微 2% 位移、淡入淡出与 easeOutCubic。
- 移除前后两页同时大幅滑动和全屏 Stack 合成，降低每日训练页切换时的绘制压力。
- 动作卡片错峰动画适当拉开间隔，让页面切换先稳定，再逐条呈现训练项目。

## v6.8.2 (2026-07-16)

### 动画与完成态精修
- 恢复周一至周日切换时的轻量水平翻页动画，并根据前进/后退方向呈现对应滑动方向。
- 恢复健身动作卡片逐条错峰出现，采用短时淡入缩放并保留独立绘制隔离。
- 完成动作后，次数和休息时间与动作名、组数同步变暗，完成态视觉保持一致。
- 保留卡片内容平滑收缩与恢复，避免过度弹跳和整页重复重绘。

## v6.8.1 (2026-07-16)

### 全面视觉与流畅度回归
- 以 v6.6.1 为基准恢复 Inter 字体体系和系统中文回退，修复中文、数字、组数与 `×` 显示不协调。
- 重新设计动作卡片完成态：平滑收起肌群与说明，保留清晰的动作名称和组次信息。
- 移除训练动作列表逐项入场动画，增加绘制隔离，降低切日和滚动时的掉帧风险。
- 数字使用等宽数字特性，组数与乘号分层排版，提升对齐和精致度。

## v6.8.0 (2026-07-16)

### 训练页性能与计划设置
- 移除每个健身动作后方的「记录」入口、最近重量与 1RM 展示，减轻训练列表构建和滚动负担。
- 设置页新增「训练计划」，可修改动作名称、组数、次数、休息时间与动作说明。
- 当前周一至周日健身安排作为内置默认计划，支持一键恢复默认。
- 自定义训练计划保存到本地，并纳入既有数据导出备份范围。

## v6.7.1 (2026-07-16)

### 严重回归修复
- 恢复训练活动整张卡片的点击完成/取消能力，不再局限于左侧小圆圈。
- 增加真实 Widget 回归测试，覆盖完成状态切换与本地持久化。
- 内置 Noto Sans SC 中文字体，统一中文显示效果，移除运行时 Google Fonts 依赖。

## v6.7.0 (2026-07-15)

### 基础能力
- 建立动作训练日志的数据模型与本地存储，覆盖重量、组数、次数、RPE、最近记录和 Epley 1RM 估算。
- 建立体重与身体围度记录的按日更新、查询和删除能力。
- 建立训练页周一至周日横向滑动切换的基础交互。
- JSON 备份格式升级为 schema v2，支持校验、v6.6 旧备份迁移、导入前备份以及合并/覆盖导入。

### 测试
- 新增 models、历史归档、数据服务和训练日横向滑动测试，使用 SharedPreferences mock 隔离本地存储。

- Android 版本统一为 `6.7.0+72`，CI 增加格式检查、静态分析、测试、arm64 APK 构建与 tag 正式签名流程。

## v6.6.1 (2026-07-10)

- 新增设置页与个人资料管理。
- 新增体重趋势图和体重记录。
- 新增训练数据 JSON 导入/导出。
- 新增 1RM 估算。
- 修复设置页主题选择与体重图表显示细节。


## v6.5.1 (2026-05-12)

### 修复
- 修复周一到周日滑动药丸在周日位置错位、溢出遮挡的问题。
- 药丸位置现在按内层实际可用宽度计算，不再受外层 padding 影响。
- 减小药丸阴影并开启裁剪，避免末尾阴影遮挡边界。

## v6.5.0 (2026-05-12)

### 顶级美学优化
- 周一到周日选择器改为类似底部导航的滑动药丸动画，选中态通过渐变滑块移动，不再只是单个按钮变色。
- 完成进度、勾选状态、日历热力图统一改为主题协调色，不再固定使用突兀绿色。
- 重新打磨全局卡片圆角、边框透明度、底部导航阴影和顶部标签质感，整体更统一、更高级。
- 训练进度条和完成数字改用主题完成色，视觉语义更清晰。
- 保留 v6.4.3 的动作文案、营养卡片和真实日期归档修复。

## v6.4.3 (2026-05-12)

### 根据大王反馈修复
- 顶部目标标签去掉“主题”字样，继续作为隐藏换主题彩蛋入口。
- 统一优化所有训练动作下方指导文案，改成更一致的“动作要点/安全提示”表达。
- 去掉周一到周日内容区切换的 `AnimatedSwitcher`，避免切换时新旧页面叠影和不流畅。
- 重做营养饮食顶部数值卡片：改成 2x2 小卡片布局，数值和单位分层显示，不再连成一串。

## v6.4.2 (2026-05-12)

### 文案优化
- 修正首页 BMI：`72.5kg / 176cm` 对应 BMI 约 `23.4`，不再显示 `24.2`。
- 首页目标按钮补充“主题”提示，降低隐藏主题入口的困惑感。
- 修正训练说明中与目标肌群矛盾的表达：下肢 A 更聚焦股四头，下肢 B 不再写死重量。
- 饮食建议从“练后 30min 内”调整为更稳妥的“训练后 1-2 小时内”。
- 渐进超负荷说明补充“动作稳定、恢复充足”的前提，避免盲目加重。

### 交互与动画
- 减轻入场动画缩放幅度和延迟，减少列表切换时的弹跳感。
- 底部导航选中动画从强回弹改为更干净的 easeOutCubic。
- 记录页切换月份时设置加载态，避免旧月份数据短暂残留。

## v6.4.1 (2026-05-12)

### 修复
- **训练记录日期错误**：修复 v6.4.0 将“周几索引 0-6”误当作“月日期 1-31”写入历史的问题；现在完成记录会按 ISO 周转换为真实日期。
- **取消勾选同步**：取消某天动作后，月度历史会同步移除/更新该日期，不再残留虚假完成记录。
- **跨年 ISO 周**：修正年初/年末跨周时周数与周所属年份计算，避免记录归档到错误年份。
- **当前月双重统计**：记录页当前月显示改为用当前周真实日期覆盖历史值，避免重复相加。
- **全年概览高亮**：只在查看当前年份时高亮当前月份。

### 优化
- 训练记录历史 helper 重构，增加真实日期归档、非法旧数据过滤和旧接口兼容。
- 版本号更新为 `6.4.1+65`。

## v6.4.0 (2026-05-01)

### 新增功能
- **每周自动重置**：基于 ISO 8601 周数检测，每周一自动清空完成记录，旧数据自动归档到月度历史
- **训练记录页面**：月度日历热力图 + 年度完成统计 + 12个月迷你概览，支持月份切换查看历史

### 修复与优化
- **修复安装版本号**：build.gradle 中 versionCode/versionName 仍硬编码为 6.2.0，已更新为 6.4.0（这是安装时显示旧版本号的根本原因）
- **顶部标题栏**：移除版本号显示，保持界面简洁
- **底部导航栏**：由5Tab改为4Tab（训练/饮食/超负荷/记录），记录移至第4位
- **主题选择改为隐藏彩蛋**：点击顶部「目标 68-70kg」按钮弹出底部主题选择面板
- **训练记录页面排版修复**：月度日历热力图周标题与天数对齐、全年概览网格宽度修正

### 技术细节
- 存储字段：`recomp_done_v6`（完成记录）+ `recomp_done_v6_week`（周数）+ `recomp_done_v6_year`（年份）+ `recomp_history_YYYY_MM`（月度历史）
- 历史数据格式：`{"D_0": 5, "D_2": 8}`（D_0=周一，value=完成动作数）

## v6.3.0 (2026-05-01)

### 训练计划调整
- **周二（下肢 A）**：调整为股四头肌/前侧重点（倒蹬机、坐姿腿屈伸、内收机）
- **周四（下肢 B）**：调整为臀腿后链/稳定重点（哈克深蹲、坐姿腿弯举、外展机）

## v6.2.0

### 基础版本
- Flutter 单文件架构，玻璃态 UI + 6套主题
- 7天训练计划（周一至周日）
- 饮食营养建议 + 渐进超负荷指南
- SharedPreferences 本地存储完成状态
